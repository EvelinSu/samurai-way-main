import {Property} from "csstype";

export type TSTitleProps = {
    color?: string
}

export type TSTextProps = {
    isEllipsis?: boolean,
    opacity?: string | number,
    fontWeight?: Property.FontWeight,
}