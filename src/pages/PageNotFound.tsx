import React from 'react';
import {SSiteContent} from "../layout/styled";

const PageNotFound = () => {
    return (
        <SSiteContent>
            Page not found
        </SSiteContent>
    );
};

export default PageNotFound;
